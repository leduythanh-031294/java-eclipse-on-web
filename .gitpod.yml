image: gitpod/workspace-full-vnc

tasks:
  - init: echo "Chuan bi moi truong Eclipse..."
    command: |
      sudo apt-get update
      sudo apt-get install -y eclipse
      eclipse

ports:
  - port: 6080
    onOpen: open-preview
